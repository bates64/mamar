* Make SegmentMap accessible, allow selection/deletion/creation
* Tracker:
    * Make sure is accessible
    * Add, move, select, delete commands etc.
    * Allow configuration of all command types
* Port over all features from previous version to mamar-web
    * Modify segment order & add new ones
    * Modify track settings (instrument, volume, pan, etc.)
* Live highlight currently-playing segment/command
    * Needs bgm_encode to return some json with info of what was written where
* CI & release
* canvas/pixijs-based piano roll to replace tracker for some users
